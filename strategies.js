const passport = require('passport'),
      localStrategy = require('passport-local'),
      {Strategy} = require('passport-jwt'),
      {jwt} = require('./config')

module.exports = () => {
//   passport.use(jwt, new Strategy(jwt,(verifiedObject, next)=>
//
// ))


}
